{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "crons": [
    {
      "path": "/api/cron/abandoned-draft",
      "//": "Daily at 09:00 UTC (14:30 IST). Hobby plans allow only one run per day — an hourly schedule is rejected at deploy time, not at runtime, so it fails the whole deployment. A nudge about an unfinished invitation is a daily-cadence thing anyway; hourly would be pestering.",
      "schedule": "0 9 * * *"
    }
  ]
}
